#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Step 1: Run tests
echo "📝 Running tests..."
npm test || {
  echo "❌ Tests failed. Please fix the issues before committing."
  exit 1
}

# Step 2: Run lint
echo "🔧 Running linter..."
npm run lint || {
  echo "❌ Linting failed. Please fix the issues before committing."
  exit 1
}

# Step 3: Run type-check
echo "📘 Running type check..."
npm run type-check || {
  echo "❌ Type check failed. Please fix the issues before committing."
  exit 1
}

# Step 4: Run lint-staged (format and auto-stage)
echo "✨ Formatting files and auto-staging..."
npx lint-staged || {
  echo "❌ Formatting failed. Please fix the issues before committing."
  exit 1
}

echo "✅ All pre-commit checks passed!"
